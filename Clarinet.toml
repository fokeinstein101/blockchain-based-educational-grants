[project]
name = 'blockchain-based-educational-grants'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.grant-disbursement]
path = 'contracts/grant-disbursement.clar'
clarity_version = 3
epoch = 'latest'

[contracts.grant-registry]
path = 'contracts/grant-registry.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
